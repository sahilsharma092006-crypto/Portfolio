rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /projects/{allPaths=**} {
      allow read: if true;
      // Only allow uploads from your authorized email
      allow write: if request.auth != null && request.auth.token.email == "your-email@gmail.com";
    }
  }
}
